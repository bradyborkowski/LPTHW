# sets age var to equal input function; prompts user with, "How old are you? "
age = input("How old are you? ")
# sets height var equal to input function; promts user with string
height = input("How tall are you? ")
# sets weight var
weight = input("How much do you weigh? ")

# prints fstring which calls on the variables defined; values depend on user's input
print(f"So, you're {age} old, {height} tall and {weight} heavy.")
